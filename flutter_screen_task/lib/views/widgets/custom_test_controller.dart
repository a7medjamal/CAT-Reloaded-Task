import 'package:flutter/material.dart';

class CustomTextController extends StatelessWidget {
  const CustomTextController({super.key});

  @override
  Widget build(BuildContext context) {
    return CustomTextController(
      
    );
  }
}